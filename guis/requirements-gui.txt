# Dependências da GUI UVR5, ajustadas para Python 3.10-3.12
# Substitui o requirements.txt original do UVR5 (que tem pins de 2022 incompatíveis com 3.12)
#
# torch e onnxruntime são instalados separadamente pelo launcher (CUDA ou CPU)
# audio-separator (CLI) também vem do install separado
#
# Foco: só o que o UVR5 REALMENTE importa (verificado via grep nos imports de UVR.py, separate.py, lib_v5/).
#
# IMPORTANTE: tkinter vem do SISTEMA (apt install python3-tk), não do pip!
#   Ubuntu 24.04+ / Debian 12+: sudo apt install -y python3-tk python3-dev

# --- Áudio core ---
soundfile>=0.12
librosa>=0.10
audioread>=3.0.1
numpy>=1.26,<3.0
scipy>=1.11

# --- Torch / ML (torch pelo launcher) ---
julius>=0.2.7
resampy>=0.4.2

# --- Tkinter GUI ---
Pillow>=10.0
pyglet>=2.0
psutil>=5.9
natsort>=8.0
pydub>=0.25
omegaconf>=2.3
ml_collections>=0.1.1
einops>=0.7
diffq>=0.2
kthread>=0.2
pyperclip>=1.8
TkinterDnD2>=0.4
wget>=3.2
six>=1.16
tqdm>=4.66
pyyaml>=6.0
screeninfo>=0.8

# --- Cryptography ---
cryptography>=41.0

# --- matchering (mastering) ---
matchering>=2.0.6

# --- onnx2pytorch (NÃO onnx2torch) ---
onnx2pytorch>=0.4.1
onnx>=1.14

# --- pytorch_lightning (UVR5 lib_v5 usa) ---
pytorch_lightning>=2.0,<3.0

# --- playsound (chime de fim) ---
playsound>=1.2.2,<1.3.0

# --- soundstretch, samplerate, pyrubberband ---
# Removidos: quebram em Python 3.12 sem libs do sistema.
# Se precisar: sudo apt install -y libsamplerate0-dev cmake && pip install samplerate soundstretch pyrubberband
